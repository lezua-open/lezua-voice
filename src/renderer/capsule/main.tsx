import React from 'react';
import ReactDOM from 'react-dom/client';
import { CapsuleApp } from './capsule-app';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CapsuleApp />
  </React.StrictMode>,
);
